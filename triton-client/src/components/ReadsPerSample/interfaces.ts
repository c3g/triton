import { DatasetState } from "@store/datasets"

export default interface ReadsPerSampleProps {
    datasetId: DatasetState["id"]
}

export interface ReadsPerSampleButtonProps {
    datasetId: DatasetState["id"]
}
